matplotlib>=3.5.0
numpy>=1.21.0
rasterio>=1.3.0
Pillow>=9.0.0
pyproj>=3.0.0

# Note: PDAL is a system-level dependency
# and must be installed separately (typically via QGIS installation).
# GDAL command-line tools are bundled separately for standalone builds.
